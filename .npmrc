# Force dev dependencies to install even when NODE_ENV=production.
# The build step needs build-only tools (tsx, vite, esbuild) that live in
# devDependencies. Render sets NODE_ENV=production, which would normally make
# `npm install` skip them and cause the build to fail with "tsx: not found".
include=dev
